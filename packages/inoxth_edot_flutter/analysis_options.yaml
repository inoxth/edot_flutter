# The analyzer resolves the nearest options file, so this one governs the
# package. It exists only to pull in the repo-wide config — strict language
# modes, the extra lints, and public_member_api_docs — which a bare
# `flutter_lints` include (the Flutter template default) would otherwise shadow.
# No deviations: this is the package the strict settings are for.
include: ../../analysis_options.yaml
