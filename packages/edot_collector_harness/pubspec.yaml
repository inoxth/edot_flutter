name: edot_collector_harness
description: >-
  Seam 2 test harness. Runs an OpenTelemetry Collector and reads back the
  telemetry the Plugin exported to it. Test infrastructure only, never published.
version: 0.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
